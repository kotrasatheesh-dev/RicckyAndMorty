query GetCharacters {
    characters {
        results {
            ...character
        }
    }
}

fragment character on Character {
    name
    id
    image
    location {
        name
        id
    }
}
